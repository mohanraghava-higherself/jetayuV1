# Copy this file to .env and fill in your values

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key
# IMPORTANT: Use service role key for backend (bypasses RLS)
# Find it in Supabase Dashboard > Project Settings > API > service_role key
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key

# Optional: Use Gemini instead
# GEMINI_API_KEY=your-gemini-api-key
# LLM_PROVIDER=gemini

# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS Origins (comma-separated, for production add your frontend URL)
# CORS_ORIGINS=https://your-frontend.com,https://www.your-frontend.com

# Email Configuration (for booking notifications to operators)
# Using Resend API (https://resend.com) - HTTP-based email service
# Get your API key from: https://resend.com/api-keys

# Resend API Key (starts with "re_")
RESEND_API_KEY=re_your-resend-api-key

# From email address shown in notifications
# For development: use "onboarding@resend.dev" (Resend's default domain)
# For production: use your verified domain (e.g., "concierge@flyjetayu.com")
# IMPORTANT: Domain must be verified in Resend dashboard before use
FROM_EMAIL=onboarding@resend.dev

# Operator emails (comma-separated) - receives booking notifications
OPERATOR_EMAILS=operator1@yourcompany.com,operator2@yourcompany.com

# Optional: Test email recipient for startup connection test
# RESEND_TEST_EMAIL=delivered@resend.dev

